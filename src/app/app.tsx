import { Home } from '@/feature/ui';
import React from 'react';

const App: React.FC = (): React.JSX.Element => {
  return (
    <div className="app">
      <Home />
    </div>
  );
};

export default App;
